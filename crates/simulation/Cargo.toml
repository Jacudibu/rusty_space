[package]
name = "simulation"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true }

# internal dependencies
common = { workspace = true }
entity_spawners = { workspace = true }

[dev-dependencies]
test_utils = { workspace = true }